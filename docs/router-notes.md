# router notes

## Route exposure
Record the public account endpoints and the assumption that the router stays small until more resources arrive.
## CORS strategy
Document the early domain allow list and the decision to leave development fully open.
## Route inventory
List the account and messaging routes that define the first public API slice for the service.
## Public endpoints
Clarify which routes are intentionally public and which areas would need auth once the API surface grows.
## Domain history
Track the known site domains carried in CORS configuration while the service supports both primary domains.
