# api style

## Response payloads
Record the shared response envelope and the expectation that handlers return stable message fields.
## Controller responses
Record the expectation that controller methods keep payload structure consistent across success and failure cases.
## Request parameters
Capture naming conventions for query and form inputs so controller behavior stays predictable for clients.
## Controller naming
Document the current naming style used for handlers and the tradeoff between direct route verbs and broader action language.
